# Optional: GitHub personal access token (increases rate limit 60 → 5000 req/hr)
# Create at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=

# Report output directory (default: reports)
# REPORTS_DIR=reports

# Data directory for history tracking (default: data)
# DATA_DIR=data
